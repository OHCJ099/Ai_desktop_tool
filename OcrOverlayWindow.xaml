<Window x:Class="AI_desktop_tool.OcrOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OCR 截图" 
        WindowStyle="None" 
        WindowState="Maximized" 
        Topmost="True" 
        AllowsTransparency="True" 
        Background="Transparent"
        Cursor="Cross"
        Loaded="Window_Loaded"
        KeyDown="Window_KeyDown"
        MouseDown="Window_MouseDown"
        MouseMove="Window_MouseMove"
        MouseUp="Window_MouseUp">
    <Grid Name="RootGrid">
        <!-- 底层展示全屏截图 -->
        <Image Name="BackgroundImage" Stretch="Fill" HorizontalAlignment="Left" VerticalAlignment="Top"/>
        
        <!-- 遮罩层，完全透明以捕获输入 -->
        <Grid Name="MaskGrid" Background="Transparent">
            <Canvas>
                <!-- 框选的红色矩形边框 -->
                <Rectangle Name="SelectionRect" Stroke="Red" StrokeThickness="2" Visibility="Collapsed"/>
            </Canvas>
        </Grid>
    </Grid>
</Window>
